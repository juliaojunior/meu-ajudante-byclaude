{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qoE '\"file_path\"[[:space:]]*:[[:space:]]*\"[^\"]+\\.tsx?\"'; then npx tsc --noEmit 2>&1 | head -30; fi",
            "shell": "bash",
            "timeout": 90,
            "statusMessage": "Type-checking..."
          }
        ]
      }
    ]
  }
}
